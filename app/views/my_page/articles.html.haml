.section
  = render 'header_tab'

  %ul.collection{style: "margin-top: 0; padding-top: 0;"}
    - if @articles.present?
      = render @articles
    - else
      %li.collection-item.center.grey-text.text-lighten-2 投稿がありません
